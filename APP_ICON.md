# Term Anywhere app icon

The mint terminal prompt identifies the app's main function. The open teal orbit suggests remote access and return to a saved session. Broad shapes and a dark background keep the mark clear at small sizes.

## Asset

- File: `term-anywhere-app-icon.png`
- Format: PNG, 1254 × 1254 pixels, opaque RGB.
- The background fills the square. There is no outer corner mask, frame, name, or wordmark.
- Use the full square when you export the 1024 × 1024 app-icon asset. Let the system apply its corner shape.
- Generated with the built-in imagegen tool. The source is unchanged.

## Generation prompt

```text
Use case: logo-brand
Asset type: final square iPhone and iPad app icon artwork for Term Anywhere, a focused native SSH terminal app.
Primary request: Design one polished, memorable terminal icon. A bold terminal prompt chevron and a short solid cursor bar form the central symbol. A single clean open circular path curves around the prompt, suggesting remote access and returning to the same session from anywhere. Keep the prompt dominant and the orbit subordinate; the whole symbol should read as one balanced mark.
Style/medium: precise, restrained, contemporary vector-like app icon with crisp geometry, smooth edges, and a very subtle premium depth. Use broad shapes, generous negative space, and no fine detail. Readable at 32 pixels.
Composition/framing: one centered icon on a full-bleed square canvas, 1024 by 1024 pixels. Central artwork occupies approximately 62 percent of the canvas, leaving a broad safe margin. No layout sheet or mockup.
Color palette: deep ink-blue background with a restrained dark tonal gradient; a bright soft mint terminal prompt and cooler teal open orbit. Strong contrast.
Constraints: the background fills every pixel right into all four square corners and is fully opaque. Do not round, cut out, frame, or mask the outer canvas. No app name, letters, words, numbers, watermark, border, device, terminal window frame, or extra objects. The chevron and cursor are abstract terminal symbols, not rendered typography. Avoid tiny dots, multiple concentric rings, network nodes, busy circuit imagery, glossy plastic, heavy glow, bevels, and cast shadows.
Generate only the finished icon image.
```

The tool returned 1254 × 1254 pixels despite the requested size. Use a normal resize during app-asset preparation; do not crop the artwork.
